# The compiler core needs NOTHING beyond the standard library.
# `python3 -m kgq.cli compile` and `ask` run on a bare CPython 3.11.
#
# These are for the Demo 0.2 workbench and the document analysers only.
fastapi>=0.110
uvicorn>=0.27
python-multipart>=0.0.9     # FastAPI reads multipart uploads through this
pdfplumber>=0.11            # PDF text extraction. No OCR, by design.
python-docx>=1.1            # DOCX paragraphs and tables

# Test-only: building real PDF/DOCX fixtures rather than mock bytes.
reportlab>=4.0
